comment: false

coverage:
    status:
        patch:
            default:
                target: auto
        project:
            default:
                target: auto

github_checks:
    annotations: false
